spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mybatis_board?serverTimezone=Asia/Seoul
    username: root
    password: mypassword
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8081